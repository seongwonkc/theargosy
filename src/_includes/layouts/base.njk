<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title or site.title }}</title>
  <meta name="description" content="{{ summary | safe }}">
  <link rel="icon" href="/assets/favicon.ico">
  <style>
    :root { --max: 1080px; --g: 1rem; font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; }
main { max-width: var(--max); margin: 2rem auto; padding: 0 var(--g); }

/* Article typography */
article { max-width: 760px; margin: 0 auto; }
article header h1 { font-size: clamp(1.8rem, 2.5vw + 1rem, 2.6rem); line-height: 1.15; margin: .25rem 0 1rem; }
article .muted { color: #666; font-size: .95rem; }
article .content { font-size: 1.06rem; line-height: 1.7; color: #202124; }
article .content p { margin: 1rem 0; }
article .content h2 { margin-top: 2rem; font-size: 1.4rem; }
figure.hero { margin: 1rem 0 1.5rem; }
figure.hero img { width: 100%; height: auto; border-radius: 10px; display:block; }
figure.hero figcaption { color: #777; font-size: .9rem; margin-top: .5rem; }
  </style>
</head>
<body>
  <header>
    <div class="wrap topbar">
      <div class="brand">
  <a href="/" style="color:#fff; text-decoration:none; display:flex; align-items:center; gap:.5rem;">
    {% if site.logo %}<img src="{{ site.logo }}" alt="{{ site.title }}" style="height:24px; width:auto;">{% endif %}
    <span>{{ site.title }}</span>
  </a>
</div>

      <nav>
        {% for item in site.navigation %}
          <a href="{{ item.href }}">{{ item.label }}</a>
        {% endfor %}
      </nav>
    </div>
  </header>

  <main>
    {{ content | safe }}
  </main>

  <footer>
    <div class="wrap muted">
      <div>{{ site.title }} — {{ site.tagline }}</div>
      <div>&copy; {{ "now" | dateReadable }} • {{ site.author }}</div>
    </div>
  </footer>
</body>
</html>
